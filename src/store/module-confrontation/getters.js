/**
 * Set value the table questionRound
 * @param  {Object} state
 * @return {Array} table data
 */
export function confrontationsdGetter (state) {
  let confrontations = state['confrontations']
  return confrontations
}
/**
 * Set value the table questionRound
 * @param  {Object} state
 * @return {Array} table data
 */
export function allConfrontationsdGetter (state) {
  let confrontations = state['allConfrontations']
  return confrontations
}
